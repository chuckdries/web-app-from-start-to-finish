{% extends 'layout.twig' %}

{% block body %}
  <h1>{{title}}</h1>
  <p>Welcome to {{title}}</p>
  {% for post in posts %}
    <h3>{{ post.author }}</h3>
    <p>{{ post.message }}</p>
  {% endfor %}

  <form method="POST" action="/posts">
  
  </form>
{% endblock %}
